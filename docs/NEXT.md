# Store Canary Next

## Unblocked

1. Add a deterministic check that tracked paths have no case-insensitive collisions.
2. Add a static-export check that every generated HTML document has one non-empty title and one character-set declaration.
3. Add a static-export check that `aria-labelledby` and `aria-describedby` references resolve to IDs in the same document.
4. Add a deterministic check that public export paths contain no spaces, control characters, or URL-reserved characters.
5. Add a static-export check that every generated HTML document has one `rel=canonical` link.
6. Add a static-export check that generated HTML contains no inline event-handler attributes.
7. Add a static-export check that generated links and resource URLs never use the `javascript:` scheme.

## Waits on Erik

- Configure the Google Workspace secondary domain and authenticated mail DNS, then approve any access-CTA address change.
- Approve any commercial terms or payment work; the current site must not accept payment.
- Approve and perform production/Cloudflare deployment and owner-led participant outreach.
