# Store Canary Next

## Unblocked

1. Add a no-deploy workflow-lint check for GitHub Actions syntax, expressions, and embedded shell.
2. Add Markdown linting and link checks for repository documentation.
3. Add a CI regression check that keeps workflow permissions read-only and prevents deploy commands from entering the quality job.

## Waits on Erik

- Configure the Google Workspace secondary domain and authenticated mail DNS, then approve any access-CTA address change.
- Approve any commercial terms or payment work; the current site must not accept payment.
- Approve and perform production/Cloudflare deployment and owner-led participant outreach.
